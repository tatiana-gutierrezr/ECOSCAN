<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo con bordes redondeados y color blanco cuando está seleccionado -->
    <item android:drawable="@drawable/round_white_background" android:state_checked="true"/>
    <!-- Fondo transparente cuando no está seleccionado -->
    <item android:drawable="@android:color/transparent"/>
</selector>